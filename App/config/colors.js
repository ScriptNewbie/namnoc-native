const lightModeColors = {
  soft: "grey",
  apply: "green",
  discard: "red",
  primary: "black",
  background: "white",
  white: "white",
};

const colors = {
  light: lightModeColors,
};

export default colors;
